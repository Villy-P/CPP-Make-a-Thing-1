#ifndef SRC_DEBUGCONSOLE_HPP
#define SRC_DEBUGCONSOLE_HPP

#include <vector>
#include <string>

namespace debug_console {
    class DebugConsole {
        public:
            static inline std::vector<std::string> elements;
    };
}

#endif